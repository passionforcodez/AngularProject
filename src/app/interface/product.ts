export interface Product {
  itemId: string;
  name: string;
  description: string;
  price: string;
  img: string;
}

export interface CartItem {
  itemId: string;
  name: string;
  description: string;
  price: string;
  quantity: number;
}

  